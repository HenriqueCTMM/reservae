{
  "rules": {
    ".read": "now < 1783911600000",
    ".write": "now < 1783911600000",
    "tables": {
      ".indexOn": ["numero", "status"]
    },
    "reservations": {
      ".indexOn": ["usuarioId", "mesaId", "data", "status"]
    },
    "messages": {
      ".indexOn": ["usuarioId", "status"]
    },
    "users": {
      ".indexOn": ["email", "perfil"]
    },
    "operatingHours": {
      "exceptions": {
        ".indexOn": ["date", "open"]
      }
    }
  }
}
